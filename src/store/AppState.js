/**
 * AppState.js — Estado global de la aplicación
 *
 * Almacena el estado compartido entre módulos:
 * módulo activo, usuario, preferencias de UI.
 * Sigue el patrón Singleton.
 */

export class AppState {}
